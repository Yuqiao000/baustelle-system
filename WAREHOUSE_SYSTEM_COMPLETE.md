# 完整仓库管理系统 (Complete Warehouse Management System)

## 📦 系统概览

这是一个为建筑公司设计的完整仓库管理系统，支持德语界面，包含以下核心功能：

### 核心功能模块

1. **架位管理** (Regal Management) 🏷️
2. **材料管理** (Material Management) 📦
3. **项目管理** (Projekt Management) 🏗️
4. **申请流程** (Anfrage Process) 📋
5. **出入库管理** (Warehouse Operations) 📊
6. **签字功能** (Mobile Signature) ✍️
7. **统计分析** (Statistik) 📈

---

## 1. 架位管理 (Regal Management)

### 1.1 架位二维码 (Regal-QR)

每个架子都有唯一的二维码，用于快速定位和管理。

**特点：**
- ✅ 固定在架子上，材料移动时二维码不动
- ✅ 扫描二维码即可查看该架位上的所有材料
- ✅ 支持快速材料定位

**数据表：** `storage_locations`

```sql
- name: 架位名称（如 "A1", "B2"）
- qr_code: 唯一二维码（如 "LOC-A1"）
- zone: 区域（如 "Zone A"）
- description: 描述
- bemerkung: 特殊备注
```

### 1.2 多材料摆放

**一个架位可以存放多种材料**

例如：A1 架位可以同时存放：
- Stromkabel 10mm (100m)
- Stromkabel 16mm (50m)
- Stromkabel 25mm (30m)

**数据表：** `inventory`

```sql
- item_id: 材料ID
- location_id: 架位ID
- quantity: 数量
- bemerkung: 备注（如："多种型号混放"）
```

### 1.3 架位备注 (Bemerkung)

用于记录特殊情况：
- 多型号电缆混放
- 易碎物品提醒
- 特殊存储要求

---

## 2. 材料管理 (Material Management)

### 2.1 材料主数据 (Materialstamm)

**字段：**
- **Hauptname** (主名称): 如 "A1.B", "Stromkabel 10mm"
- **Aliase** (别名): 多个别名，支持模糊搜索
- **Kategorie** (分类): Kabel, Muffen, Schutzkleidung...
- **Einheit** (单位): m, Stück, kg...
- **Min-Stock** (最低库存): 警戒线
- **Current Stock** (当前库存): 实时库存
- **Material-QR** (材料二维码): 高频使用材料的专用二维码
- **Bilder** (图片): 帮助识别的图片

### 2.2 材料二维码协调方案

**两种二维码并存：**

1. **架位二维码** (Regal-QR)
   - 固定在架子上
   - 用于定位和盘点
   - 扫描后显示该架位所有材料

2. **材料二维码** (Material-QR)
   - 贴在高频使用材料上
   - 直接扫描快速申请
   - 自动关联到当前架位

**使用场景：**
- 工人扫描材料QR → 快速申请该材料
- Lager扫描架位QR → 查看/管理该架位所有材料
- Lager扫描材料QR → 快速出库该材料

---

## 3. 项目管理 (Projekt)

### 3.1 项目基本信息

**数据表：** `projects`

```sql
- name: 项目名称
- project_number: 项目编号（唯一）
- description: 项目描述
- start_date: 开始日期
- end_date: 结束日期
- bauleiter_id: 项目负责人
- is_active: 是否激活
```

### 3.2 项目材料追踪

每个项目可以追踪：
- 使用的所有材料
- Sub 使用情况
- 材料调拨记录
- 退货记录

**视图：** `project_material_stats`
- 显示每个项目的材料使用统计
- 出库数量、退货数量、净使用量

---

## 4. 申请流程 (Anfrage)

### 4.1 提交方式

**两种申请方式：**

1. **工人自助申请**（手机APP）
   - 扫描材料QR或手动选择
   - 自动填充项目和申请人信息（上次记录）
   - 输入数量和备注

2. **Lager代为申请**
   - 工人不会使用APP时
   - Lager帮忙提交申请

### 4.2 申请单字段

**扩展的 `requests` 表：**

```sql
-- 基本信息
- request_number: 申请单号
- worker_id: 申请人
- baustelle_id: 工地
- project_id: 项目（新增）

-- 工人类型
- worker_type: 'intern' 或 'sub'（新增）
- subcontractor_id: Sub承包商ID（新增）

-- 状态
- status: 流程状态（见下方）
- priority: 优先级

-- 时间
- needed_date: 需要日期
- delivery_time: 交付时间段

-- 签字
- signature_data: 签名数据 Base64（新增）
- signed_at: 签字时间（新增）
- picked_up_at: 领取时间（新增）

-- 备注
- notes: 备注
```

### 4.3 申请流程状态

```
pending          → Neu (新申请)
    ↓
confirmed        → 已确认
    ↓
preparing        → In Bearbeitung (仓库处理中)
    ↓
ready            → Bereit zur Abholung (可取货)
    ↓
picked_up        → Abgeholt (已领取 & 签字)
    ↓
completed        → 已完成
```

### 4.4 自动填充功能

**工人提交申请时：**
- 自动填充上次使用的项目
- 自动填充申请人信息
- 减少重复输入

**实现方式：**
- 前端记录 localStorage
- 后端查询该工人的最近申请

---

## 5. 签字功能 (Mobile Signature)

### 5.1 取货签字流程

**工人领取材料时：**

1. Lager将状态改为 "Bereit zur Abholung"
2. 工人到达仓库
3. **工人在手机上签字**
4. 系统记录：
   - 签名数据（Base64图片）
   - 签字时间
   - 领取材料清单
   - 数量确认

### 5.2 签名数据存储

```sql
-- requests 表
signature_data: TEXT  -- Base64 编码的签名图片
signed_at: TIMESTAMPTZ -- 签字时间
picked_up_at: TIMESTAMPTZ -- 实际领取时间
```

### 5.3 前端实现

使用 Canvas API 或签名库（如 `react-signature-canvas`）：
```javascript
<SignatureCanvas
  onEnd={() => {
    const signatureData = canvas.toDataURL('image/png')
    submitSignature(signatureData)
  }}
/>
```

---

## 6. 出入库管理 (Warehouse Operations)

### 6.1 出入库类型

**数据表：** `inventory_transactions`

```sql
transaction_type:
  - 'in'           → 入库 (Wareneingang - 新采购)
  - 'out'          → 出库 (Warenausgang - 正常申请)
  - 'transfer'     → 调拨 (项目间调拨)
  - 'return'       → 退货 (Rückgabe - 工人退货)
  - 'adjustment'   → 调整/盘点
  - 'unauthorized' → 未经授权拿走（定期统计）
```

### 6.2 入库流程 (Wareneingang)

**包括：**
1. **新采购入库**
   - 记录供应商
   - 验收数量
   - 存放架位

2. **工人退货入库**
   - 关联退货申请
   - 检查物品状态
   - 重新入库

3. **项目调拨入库**
   - 从项目A调拨到项目B
   - 记录为B项目的入库
   - 记录调拨原因

### 6.3 退货流程 (Rückgabe)

**数据表：** `return_requests` + `return_items`

**流程：**
```
工人发起退还
    ↓
Lager审核
    ↓
  接受 / 拒绝
    ↓
若接受 → 入库并记录来源项目
```

**退货单字段：**
```sql
- return_number: 退货单号 (RET000001)
- worker_id: 退货人
- project_id: 来源项目
- status: pending / approved / rejected
- reason: 退货原因
- approved_by: 审核人
- approved_at: 审核时间
```

### 6.4 未经授权拿走登记

**场景：** 有钥匙的工人未申请直接拿走材料

**解决方案：**
1. Lager定期盘点（如每周）
2. 发现差异时创建 `unauthorized` 类型记录
3. 记录推测的拿走人和项目
4. 统计分析，提醒规范流程

**记录字段：**
```sql
- transaction_type: 'unauthorized'
- quantity: 缺少的数量
- operator_id: 推测的操作人
- project_id: 推测的项目
- notes: 备注（如："周盘点发现缺少"）
```

---

## 7. 材料调拨 (Projekttransfer)

### 7.1 调拨流程

**数据表：** `material_transfers` + `transfer_items`

**流程：**
```
Lager打电话给 Bauleiter
    ↓
Bauleiter 同意
    ↓
Lager创建调拨单
    ↓
在项目A出库 & 在项目B入库
    ↓
记录调拨日志
```

### 7.2 调拨单字段

```sql
- transfer_number: 调拨单号 (TRF000001)
- from_project_id: 源项目
- to_project_id: 目标项目
- bauleiter_approved: Bauleiter是否同意
- bauleiter_id: 批准的Bauleiter
- approved_at: 批准时间
- operator_id: 操作人（Lager）
- status: pending / approved / completed
- notes: 备注
```

### 7.3 自动化处理

调拨完成时，系统自动：
1. 在项目A创建出库记录
2. 在项目B创建入库记录
3. 更新两个项目的材料统计
4. 发送通知给相关人员

---

## 8. 统计功能 (Statistik)

### 8.1 过滤器

支持以下过滤条件：
- 📅 **日期范围** (von/bis)
- 🏗️ **项目** (Projekt / Allgemein)
- 📦 **材料** (按名称或分类)
- 👷 **工人类型** (intern / sub)
- 🏢 **Sub承包商** (具体哪个Sub)
- 👤 **申请人**
- 🔄 **操作类型** (出库/入库/调拨/退货)
- 📂 **材料分类** (Kategorie)

### 8.2 统计视图

**1. 使用频率统计** (`material_usage_frequency`)
```sql
SELECT
  item_name,
  usage_count,      -- 使用次数
  total_quantity,   -- 总数量
  last_used_at,     -- 最后使用时间
  avg_quantity      -- 平均数量
FROM material_usage_frequency
ORDER BY usage_count DESC
LIMIT 20;
```

**2. 项目材料统计** (`project_material_stats`)
```sql
SELECT
  project_name,
  item_name,
  total_out,        -- 总出库
  total_returned,   -- 总退货
  net_usage,        -- 净使用量
  transaction_count -- 交易次数
FROM project_material_stats
WHERE project_id = ?
```

**3. 最近出库记录** (Recent Warenausgänge)
```sql
SELECT *
FROM inventory_transactions
WHERE transaction_type = 'out'
ORDER BY created_at DESC
LIMIT 50;
```

### 8.3 输出形式

**1. 表格视图**
- 可排序
- 可导出 CSV / Excel
- 分页显示

**2. 柱状图**
- 按时间的使用趋势
- 按项目的材料分布
- 按分类的使用对比

**3. 饼图**
- Intern vs Sub 使用占比
- 各分类材料占比
- 各项目材料占比

---

## 9. 数据库架构总览

### 核心表

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `storage_locations` | 架位管理 | name, qr_code, zone |
| `inventory` | 库存（材料在架位上） | item_id, location_id, quantity |
| `projects` | 项目管理 | name, project_number, bauleiter_id |
| `subcontractors` | Sub承包商 | name, company_name, contact |
| `requests` | 申请单 | worker_type, project_id, signature_data |
| `inventory_transactions` | 出入库记录 | transaction_type, project_id, quantity |
| `return_requests` | 退货申请 | status, approved_by |
| `material_transfers` | 材料调拨 | from_project_id, to_project_id |

### 关键视图

| 视图名 | 用途 |
|--------|------|
| `items_with_aliases` | 材料含别名 |
| `material_usage_frequency` | 使用频率统计 |
| `project_material_stats` | 项目材料统计 |

### 辅助函数

| 函数名 | 用途 |
|--------|------|
| `search_items()` | 智能搜索材料 |
| `generate_return_number()` | 生成退货单号 |
| `generate_transfer_number()` | 生成调拨单号 |

---

## 10. 部署步骤

### 步骤 1: 运行数据库升级

在 Supabase SQL Editor 中按顺序运行：

```bash
1. database/material_management_upgrade.sql
   ↓ (创建基础材料管理功能)

2. database/warehouse_management_full.sql
   ↓ (创建完整仓库管理功能)
```

### 步骤 2: 数据初始化

运行后会自动创建：
- ✅ 示例架位 (A1, A2, B1, B2, C1)
- ✅ 德语分类 (Kabel, Muffen, etc.)
- ✅ 必要的索引和约束
- ✅ 权限策略 (RLS)

### 步骤 3: 后端 API

后端已包含材料管理 API：
- `/materials` - 材料、别名、图片
- `/api/wms` - 仓库管理（架位、出入库）

需要添加的新 API：
- `/projects` - 项目管理
- `/returns` - 退货管理
- `/transfers` - 调拨管理
- `/statistics` - 统计分析

### 步骤 4: 前端开发

需要开发的页面：
- 架位管理页面
- 项目管理页面
- 签字页面（Canvas签名）
- 退货申请页面
- 调拨管理页面
- 统计分析页面（图表）

---

## 11. 技术栈建议

### 前端库

- **签名**: `react-signature-canvas`
- **图表**: `recharts` 或 `chart.js`
- **导出**: `xlsx` (Excel导出)
- **日期**: `date-fns` (日期处理)
- **二维码**: `qrcode.react` (生成QR)
- **扫描**: 继续使用现有的 `NativeBarcodeScanner`

### 后端功能

- FastAPI 路由扩展
- 数据聚合和统计
- PDF 报表生成（可选）
- Excel 导出功能

---

## 12. 功能优先级建议

### P0 (必须先实现)
1. ✅ 材料管理（已完成）
2. ✅ 架位管理（数据库已完成）
3. ✅ 基础申请流程（已完成）
4. 出入库基础功能

### P1 (核心功能)
1. 项目管理
2. 签字功能
3. 退货流程
4. 基础统计

### P2 (增强功能)
1. 材料调拨
2. 高级统计分析
3. Sub承包商管理
4. 未经授权拿走登记

---

## 🎉 系统特色

- 🇩🇪 **全德语界面**
- 📱 **移动优先** (手机签字、扫码)
- 🏷️ **双QR系统** (架位QR + 材料QR)
- 📊 **实时统计** (使用频率、项目追踪)
- ✍️ **电子签名** (无纸化领料)
- 🔄 **完整追溯** (材料全生命周期)
- 👥 **Sub管理** (区分内部和外部)
- 🎯 **智能搜索** (别名、纠错)

---

立即运行数据库脚本开始使用！🚀
